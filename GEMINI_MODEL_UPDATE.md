# ✅ Gemini API Model Update Fix - November 2025

## 🎯 ปัญหา

ระบบไม่สามารถทำนายผลได้และแสดง error:
```
404 models/gemini-pro is not found for API version v1beta, or is not supported for generateContent
```

## 🔍 สาเหตุ

Google Gemini API ได้เปลี่ยนชื่อและยกเลิกการสนับสนุนโมเดลเก่า:
- ❌ `gemini-pro` - **ยกเลิกการใช้งานแล้ว** (deprecated)
- ❌ `gemini-1.5-flash` - ชื่อเก่าที่ไม่รองรับแล้ว
- ❌ `gemini-1.5-pro` - ชื่อเก่าที่ไม่รองรับแล้ว

ต้องเปลี่ยนมาใช้ชื่อโมเดลใหม่ที่มี `-latest` ต่อท้าย

## ✅ วิธีแก้ไข

### 1. อัปเดต Google Generative AI SDK

**ไฟล์: `requirements.txt`**
```diff
- google-generativeai==0.8.3
+ google-generativeai==0.8.5
```

### 2. เปลี่ยนชื่อโมเดลเริ่มต้น

**ไฟล์: `app.py` (บรรทัด ~1167-1170)**
```python
GEMINI_API_KEY = os.environ.get('GEMINI_API_KEY')
# Use gemini-1.5-flash-latest which is fast and widely available
# Updated to use latest model naming convention (gemini-pro is deprecated)
GEMINI_MODEL_NAME = os.environ.get('GEMINI_MODEL_NAME', 'gemini-1.5-flash-latest')
GEMINI_MAX_FILE_SIZE_MB = float(os.environ.get('GEMINI_MAX_FILE_SIZE_MB', 5))
```

### 3. อัปเดตรายการโมเดล fallback

**ไฟล์: `app.py` (บรรทัด ~1189-1191)**
```python
# Updated fallback list with current available models
for default_name in ['gemini-1.5-flash-latest', 'gemini-1.5-pro-latest', 'gemini-1.0-pro-latest']:
    _add(default_name)
```

### 4. อัปเดตไฟล์ตัวอย่าง

**ไฟล์: `.env.example`**
```env
# Gemini Model Name (default: gemini-1.5-flash-latest)
# Available models: gemini-1.5-flash-latest, gemini-1.5-pro-latest, gemini-1.0-pro-latest
# Note: gemini-pro is deprecated, use gemini-1.5-flash-latest for faster responses
GEMINI_MODEL_NAME=gemini-1.5-flash-latest
```

## 📋 โมเดลที่รองรับในปัจจุบัน

| โมเดล | คำอธิบาย | แนะนำ |
|-------|----------|-------|
| `gemini-1.5-flash-latest` | เร็ว, เหมาะสำหรับงานทั่วไป | ✅ **แนะนำ** |
| `gemini-1.5-pro-latest` | ทรงพลัง, เหมาะสำหรับงานซับซ้อน | ✅ ใช้ได้ |
| `gemini-1.0-pro-latest` | โมเดลรุ่นเก่า, ยังรองรับอยู่ | ⚠️ Legacy |

## 🚀 การทดสอบ

หลังจากแก้ไข:
1. ติดตั้งแพ็กเกจใหม่: `pip install -r requirements.txt`
2. อัปเดตไฟล์ `.env`:
   ```env
   GEMINI_MODEL_NAME=gemini-1.5-flash-latest
   ```
3. รันแอปพลิเคชัน: `python app.py`
4. ทดสอบคลิก "ทำนายผล" บนหน้า `/test`

## 📌 หมายเหตุ

- โมเดล `gemini-pro` ถูกยกเลิกและจะไม่ทำงานอีกต่อไป
- ต้องใช้ชื่อโมเดลที่มี `-latest` เท่านั้น
- SDK version 0.8.5 รองรับโมเดลใหม่เหล่านี้
- ระบบจะลอง fallback ไปยังโมเดลอื่นอัตโนมัติหากโมเดลหลักไม่พร้อมใช้งาน

## 🎉 ผลลัพธ์

✅ ระบบสามารถเรียกใช้ Gemini API ได้ปกติ  
✅ การทำนายผลทำงานได้อย่างถูกต้อง  
✅ ไม่มี error 404 อีกต่อไป  
